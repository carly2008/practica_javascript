/**
 * Di tu número favorito y pide el del usuario, y luego muestra el resultado de multiplicarlos en un
 * mensaje. Utiliza tres mensajes separados para mostrar tu número, pedir el del usuario y el
 * resultado de la multiplicación
 */

let myNum = 19
let num = parseInt(prompt('Dame tu numero favorito'))

alert(`Mi numero favorito es el ${myNum} y cuando multiplicas ambos, ${myNum} y ${num}, da ${myNum*num}`)